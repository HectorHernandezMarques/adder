get_filename_component(LibAdder_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET LibAdder::LibAdder)
    include("${LibAdder_CMAKE_DIR}/LibAdderTargets.cmake")
endif()

set(LibAdder_LIBRARIES LibAdder::libadder)